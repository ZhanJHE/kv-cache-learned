# 环境配置指南

> 本文档汇总了项目的环境配置步骤、一键部署脚本说明以及常见问题。
> 适用于 **AutoDL**（Linux + CUDA）和本地开发环境。

---

## 一、运行环境要求

| 项目 | 最低要求 | 推荐配置 |
|------|---------|---------|
| 操作系统 | Linux (Ubuntu 20.04+) | Ubuntu 22.04 |
| Python | 3.10 | 3.10 |
| CUDA | 11.8 | 12.1 |
| GPU 显存 | 16 GB | 24 GB (RTX 3090 / A100 40G) |
| 硬盘空间 | 50 GB | 100 GB（含模型与数据集） |
| 网络 | 可访问 HuggingFace / ModelScope | 镜像加速 |

---

## 二、快速开始（一键部署）

适用于 **AutoDL** 云端环境：

```bash
cd /root/autodl-tmp/kv-cache-learned
bash autodl_cloud/deploy.sh
```

此脚本会自动完成：
- 创建 `kv_cache` conda 环境（Python 3.10）
- 安装所有 Python 依赖（`autodl_cloud/requirements.txt`）
- 验证 vLLM 和 PyTorch CUDA 可用性
- 下载 ShareGPT 数据集
- 执行阶段 1→3 全链路实验

> 预计耗时：首次运行约 30~60 分钟，后续约 10~20 分钟。

---

## 三、分步配置（手动）

### 3.1 创建 Conda 环境

```bash
conda create -n kv_cache python=3.10 -y
conda activate kv_cache
```

### 3.2 安装依赖

一键安装全部依赖：

```bash
pip install -r autodl_cloud/requirements.txt
```

> **注意**：`torch==2.3.0` 附带约 2.5GB 的 CUDA 库（`nvidia-cudnn-cu12`、`nvidia-cublas-cu12` 等），首次安装需要较长时间。pip 会缓存已下载的包，中断后重新运行即可从断点继续。

<details>
<summary><b>📦 依赖清单</b></summary>

| 包 | 版本 | 用途 |
|---|------|------|
| `vllm` | `==0.5.0` | 推理框架（Hook 目标） |
| `torch` | `==2.3.0` | 深度学习框架 |
| `transformers` | `>=4.40.0` | 模型加载 |
| `datasets` | `>=2.14.0` | 数据集加载 |
| `accelerate` | `>=0.25.0` | 分布式加速 |
| `numpy` | `>=1.24.0` | 数值计算 |
| `pandas` | `>=2.0.0` | 数据处理 |
| `scikit-learn` | `>=1.3.0` | 评估指标 |
| `matplotlib` | `>=3.7.0` | 基础绘图 |
| `seaborn` | `>=0.12.0` | 统计图表 |
| `tqdm` | `>=4.65.0` | 进度条 |
| `requests` | `>=2.31.0` | HTTP 请求 |

> **版本锁定说明**：`vllm==0.5.0` 与 `torch==2.3.0` 为硬编码版本。Trace Hook 逻辑依赖 vLLM 特定版本的内部 API（`BlockManager`、`PhysicalTokenBlock`），升级可能导致 Trace 收集失效。

</details>

### 3.3 初始化目录结构

```bash
mkdir -p data/traces      # KV Cache 访问 Trace（JSONL）
mkdir -p data/features    # 预测器训练数据
mkdir -p results/figures  # 实验图表产出
mkdir -p results/logs     # 训练与模拟日志
```

### 3.4 下载数据集

```bash
cd data
# HuggingFace 直连
wget https://huggingface.co/datasets/anon8231489123/ShareGPT_Vicuna_unfiltered/resolve/main/ShareGPT_V3_unfiltered_cleaned_split.json -O sharegpt.json

# 国内镜像（ModelScope）
wget https://www.modelscope.cn/datasets/baicai/ShareGPT/resolve/master/ShareGPT_V3_unfiltered_cleaned_split.json -O sharegpt.json
```

### 3.5 下载模型

首次运行 `collect_trace.py` 时，vLLM 会自动从 HuggingFace 下载模型到缓存目录。也可提前手动下载：

```bash
# HuggingFace（需 huggingface-cli）
huggingface-cli download Qwen/Qwen2.5-7B-Instruct --local-dir ./models/qwen2.5-7b-instruct

# ModelScope（国内镜像）
pip install modelscope
modelscope download --model qwen/Qwen2.5-7B-Instruct --local_dir ./models/qwen2.5-7b-instruct
```

模型路径可通过环境变量 `KV_CACHE_MODEL` 覆盖。

---

## 四、验证安装

依次执行以下检查，确保环境就绪：

```bash
# 1. Python 版本
python --version  # 期望：Python 3.10.x

# 2. vLLM 安装
python -c "import vllm; print(vllm.__version__)"  # 期望：0.5.0

# 3. PyTorch + CUDA 可用
python -c "import torch; print(torch.cuda.is_available()); print(torch.version.cuda)"

# 4. GPU 显存识别
nvidia-smi

# 5. 完整环境验证
python -c "
import vllm; print('vLLM:', vllm.__version__)
import torch; print('PyTorch:', torch.__version__)
import transformers; print('Transformers:', transformers.__version__)
import numpy; print('NumPy:', numpy.__version__)
import pandas; print('Pandas:', pandas.__version__)
import sklearn; print('scikit-learn:', sklearn.__version__)
import matplotlib; print('Matplotlib:', matplotlib.__version__)
import seaborn; print('Seaborn:', seaborn.__version__)
import datasets; print('Datasets:', datasets.__version__)
import accelerate; print('Accelerate:', accelerate.__version__)
"
```

---

## 五、各阶段运行命令

环境配置完成后，按如下顺序执行实验：

| 阶段 | 命令 | 说明 |
|------|------|------|
| 1. 收集 Trace | `python scripts/collect_trace.py` | Hook vLLM BlockManager，生成 `data/traces/sharegpt_trace.jsonl` |
| 2. 训练预测器 | `cd predictor && python train.py && cd ..` | 训练 `ReusePredictor`，保存权重到 `predictor/reuse_predictor.pt` |
| 3. 模拟器评估 | `cd simulator && python evaluate.py && cd ..` | 对比 LRU / FIFO / Learned / Belady 命中率 |
| 一键执行 | `bash scripts/run_all.sh` | 顺序执行 1→2→3 |

> **提示**：阶段 1（Trace 收集）需要 GPU 实例。阶段 2 和 3（训练 + 模拟器）可以在 CPU 上运行。

---

## 六、目录结构

```
kv-cache-learned/
├── autodl_cloud/
│   ├── deploy.sh              # 一键部署脚本
│   ├── offline_install.sh     # 离线安装脚本
│   └── requirements.txt       # Python 依赖清单
├── data/
│   ├── sharegpt.json          # ShareGPT 数据集（~642MB）
│   ├── traces/                # KV Cache 访问 Trace（运行后生成）
│   └── features/              # 预测器训练数据
├── models/
│   └── qwen2.5-7b-instruct/   # Qwen2.5-7B 模型权重（~15GB）
├── predictor/                 # 预测器训练代码 + 权重
├── simulator/                 # 模拟器评估代码
├── scripts/
│   ├── collect_trace.py       # Trace 收集
│   ├── setup_env.sh           # 环境初始化
│   └── run_all.sh             # 一键运行 1→2→3
├── results/
│   ├── figures/               # 实验结果图表
│   └── logs/                  # 实验日志
└── real_system/               # 真实系统集成（可选）
```

---

## 七、已知问题

| 问题 | 说明 | 状态 |
|------|------|------|
| `torch==2.1.2` 与 `vllm==0.5.0` 不兼容 | 升级到 `torch==2.3.0` 后解决 | ✅ 已修复 |
| V100 不支持 Prefix Caching | `num_hashed_tokens` 恒为 0，`ref_count` 恒为 1 | ⚠️ 已知局限（需 Ampere 架构） |
| 无卡模式 GPU 不可用 | 阶段 1 需要 GPU，阶段 2/3 可在 CPU 运行 | ⚠️ 正常，有卡后重启实例即可 |
| Belady 实现局限 | `future_trace` 中同一 block_id 跨生命周期混合，不是严格上界 | ⚠️ 不影响 LRU/FIFO/Learned 对比 |
| 单一数据集/模型 | ShareGPT + Qwen2.5-7B，泛化性待验证 | ⚠️ 已知局限 |
| 离线回放 | 训练/评估用同一 Trace，真实部署需在线更新 | ⚠️ 已知局限 |

---

## 八、常见问题 (FAQ)

### Q1: 模型下载慢或失败？

- 使用 AutoDL 内置模型库（如有）
- 或通过 ModelScope 镜像下载后创建软链接

### Q2: 如何只跑阶段 2/3（跳过 Trace 收集）？

将已有的 `sharegpt_trace.jsonl` 放到 `data/traces/`，然后直接运行 `bash scripts/run_all.sh`。

### Q3: 实验结果在哪里？

```
results/
├── figures/hit_rate_comparison.png   # 命中率对比图
└── logs/simulation_results.json      # 原始数据
predictor/reuse_predictor.pt          # 训练好的预测器权重
data/traces/sharegpt_trace.jsonl      # 收集的 Trace
```

### Q4: vLLM 版本兼容性？

本项目锁定 `vllm==0.5.0`，Hook 逻辑依赖该版本的内部 API。请勿擅自升级 vLLM。

### Q5: 环境配置报错怎么办？

参考 [DEPLOYMENT.md](./DEPLOYMENT.md) 中的故障排查表，或查看 AutoDL 实例日志。
