# 环境配置指南

## 已完成

- ✅ `kv_cache` Conda 环境已创建（Python 3.10）
- ✅ 运行时目录已创建：`data/traces/`、`data/features/`、`results/figures/`、`results/logs/`
- ✅ `requirements.txt` 版本冲突已修复（`torch==2.1.2` → `torch==2.3.0`，与 `vllm==0.5.0` 兼容）

---

## 需要你手动执行的步骤

### 1. 安装依赖

```bash
conda activate kv_cache
pip install -r autodl_cloud/requirements.txt
```

> **注意**：`torch==2.3.0` 附带约 2.5GB 的 CUDA 库（`nvidia-cudnn-cu12`、`nvidia-cublas-cu12` 等），首次安装需要较长时间。pip 会缓存已下载的包，如果中断了重新运行即可从断点继续。

### 2. 验证安装

```bash
conda activate kv_cache
python -c "
import vllm; print('vLLM:', vllm.__version__)
import torch; print('PyTorch:', torch.__version__)
import transformers; print('Transformers:', transformers.__version__)
import numpy; print('NumPy:', numpy.__version__)
import pandas; print('Pandas:', pandas.__version__)
import sklearn; print('scikit-learn:', sklearn.__version__)
import matplotlib; print('Matplotlib:', matplotlib.__version__)
import seaborn; print('Seaborn:', seaborn.__version__)
import datasets; print('Datasets:', datasets.__version__)
import accelerate; print('Accelerate:', accelerate.__version__)
"
```

### 3. 目录结构确认

```
kv-cache-learned/
├── data/
│   ├── traces/      # Trace 存放位置（运行 collect_trace.py 后生成）
│   └── features/    # 训练特征（运行 train.py 后生成）
├── results/
│   ├── figures/     # 图表产出
│   └── logs/        # 实验日志
└── ...（其余源码目录）
```

### 4. 后续运行

```bash
conda activate kv_cache

# 阶段 1：收集 Trace
python scripts/collect_trace.py

# 阶段 2：训练预测器
cd predictor && python train.py && cd ..

# 阶段 3：模拟器评估
cd simulator && python evaluate.py && cd ..
```

> **提示**：由于是无卡模式，vLLM 相关操作（`collect_trace.py`）需要切换到有 GPU 的实例才能运行。预测器训练（`train.py`）和模拟器评估（`evaluate.py`）可以在 CPU 上跑。

---

## 已知问题

| 问题 | 状态 |
|------|------|
| `torch==2.1.2` 与 `vllm==0.5.0` 冲突 | ✅ 已修复为 `torch==2.3.0` |
| AGENTS.md 中写的是 `torch==2.1.2` | 需同步更新文档 |
| 无卡模式 GPU 不可用 | 正常，有卡后重启实例即可 |
